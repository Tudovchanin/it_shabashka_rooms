{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "predeploy": "rm -rf dist && npm run generate && touch dist/.nojekyll",
    "deploy": "gh-pages -d dist --dotfiles --branch gh-pages"
  },
  "dependencies": {
    "nuxt": "^3.17.2",
    "vue": "^3.5.13",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "sass": "^1.87.0",
    "sass-loader": "^16.0.5"
  }
}
